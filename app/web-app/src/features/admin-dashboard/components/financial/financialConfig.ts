export const PAYMENTS_LIVE = false;

// Default platform revenue overrides used while payments are not live
export const PLATFORM_REVENUE_OVERRIDES = {
    commissionTotal: 0,
    convenienceTotal: 0,
    adjustmentsTotal: 8400,
};

export default {
    PAYMENTS_LIVE,
    PLATFORM_REVENUE_OVERRIDES,
};
